# Planer

Современное веб-приложение с поддержкой мобильных платформ, построенное на React, TypeScript и Cloudflare Workers с полноценной системой аутентификации.

[![Production](https://img.shields.io/badge/Production-Live-brightgreen)](https://planer.moldahasank.workers.dev)
[![Firebase](https://img.shields.io/badge/Firebase-Auth-orange)](https://firebase.google.com/)
[![Cloudflare](https://img.shields.io/badge/Cloudflare-Workers-blue)](https://workers.cloudflare.com/)

## ✨ Ключевые возможности

- 🔐 **Полная аутентификация** - регистрация, вход, подтверждение email
- 🛡️ **Защищенный API** - все endpoints защищены JWT токенами
- 📧 **Email Verification** - обязательное подтверждение email перед доступом
- ⚡ **Cloudflare Workers** - быстрый и масштабируемый backend
- 💾 **Cloudflare D1** - SQLite база данных на edge
- 📱 **Мобильная поддержка** - нативное Android приложение через Capacitor
- 🎨 **Material-UI** - современный и адаптивный интерфейс
- 🔒 **Двойная защита** - проверка авторизации на frontend и backend

## 🚀 Технологии

### Frontend
- **React 19** - UI библиотека
- **TypeScript** - типизированный JavaScript
- **Vite** - быстрый сборщик и dev-сервер
- **Material-UI (MUI)** - UI компоненты
- **Firebase Auth** - аутентификация пользователей

### Backend
- **Cloudflare Workers** - серверная логика и API
- **Hono** - веб-фреймворк для Workers
- **Cloudflare D1** - SQLite база данных
- **Cloudflare KV** - хранилище для кеша
- **@hono/firebase-auth** - middleware для проверки JWT

### Mobile
- **Capacitor** - кроссплатформенная мобильная разработка
- **Android** - нативная поддержка Android

## 📋 Требования

- Node.js 18+ 
- npm или yarn
- Android Studio (для разработки Android приложения)

## 🛠️ Установка

1. Клонируйте репозиторий:
```bash
git clone https://github.com/BibolTulepbergen/Planer.git
cd planer
```

2. Установите зависимости:
```bash
npm install
```

3. Настройте Firebase (для локальной разработки):
   - Создайте проект в [Firebase Console](https://console.firebase.google.com/)
   - Включите Email/Password авторизацию
   - Скопируйте конфигурацию в `src/firebase/config.ts`
   - Добавьте `localhost` в Authorized Domains

4. Настройте Cloudflare (для production):
   - Создайте аккаунт на [Cloudflare](https://dash.cloudflare.com/)
   - Создайте D1 базу данных
   - Создайте KV namespace для кеша
   - Обновите ID в `wrangler.jsonc`

## 🏃 Разработка

### Запуск локального сервера разработки

```bash
npm run dev
```

Приложение будет доступно по адресу `http://localhost:5173`

### Сборка проекта

```bash
npm run build
```

### Предпросмотр production сборки

```bash
npm run preview
```

### Линтинг

```bash
npm run lint
```

## ☁️ Развертывание на Cloudflare

### Развертывание Worker и статических файлов

```bash
npm run deploy
```

Это команда выполнит сборку проекта и развернет его на Cloudflare Workers.

### Генерация типов для Cloudflare Workers

```bash
npm run cf-typegen
```

## 📱 Мобильная разработка (Android)

Проект использует Capacitor для создания нативных мобильных приложений.

### Синхронизация с нативными проектами

```bash
npm run cap:sync
```

### Открыть Android Studio

```bash
npm run cap:open:android
```

### Полная сборка и открытие Android проекта

```bash
npm run cap:android
```

Эта команда:
1. Соберет веб-приложение
2. Синхронизирует файлы с нативным проектом
3. Откроет Android Studio

## 📁 Структура проекта

```
planer/
├── src/                    # Исходный код React приложения
│   ├── App.tsx            # Главный компонент
│   ├── main.tsx           # Точка входа с логикой авторизации
│   ├── components/        # React компоненты
│   │   └── Auth/         # Компоненты аутентификации
│   │       ├── Login.tsx          # Форма входа
│   │       ├── Register.tsx       # Форма регистрации
│   │       ├── AuthPage.tsx       # Страница авторизации
│   │       ├── EmailVerification.tsx  # Подтверждение email
│   │       └── ProtectedRoute.tsx     # Защита маршрутов
│   ├── context/           # React Context
│   │   └── AuthContext.tsx    # Контекст авторизации
│   ├── firebase/          # Firebase конфигурация
│   │   └── config.ts      # Настройки Firebase
│   └── utils/             # Утилиты
│       └── api.ts         # API клиент с JWT
├── worker/                # Cloudflare Worker (REST API)
│   ├── index.ts          # Точка входа Worker
│   ├── types.ts          # TypeScript типы
│   ├── middleware/       # Middleware
│   │   ├── firebaseAuth.ts   # Проверка JWT токенов
│   │   ├── logger.ts         # Логирование
│   │   └── errorHandler.ts   # Обработка ошибок
│   └── routes/           # API маршруты
│       └── versions.ts   # Endpoints для версий
├── migrations/           # SQL миграции для D1
│   ├── 0001_initial.sql # Начальная схема БД
│   └── README.md        # Документация по миграциям
├── android/             # Android нативный проект
├── public/              # Статические файлы
├── dist/                # Собранные файлы
├── API_DOCUMENTATION.md # Полная документация API
├── DATABASE_SETUP.md    # Настройка базы данных
├── capacitor.config.ts  # Конфигурация Capacitor
├── wrangler.jsonc       # Конфигурация Cloudflare Workers + D1
└── vite.config.ts       # Конфигурация Vite
```

## 🔧 Конфигурация

### Environment Variables

В `wrangler.jsonc` настроены следующие переменные:

```jsonc
"vars": {
  "MY_NAME": "Moldahasank",
  "PUBLIC_JWK_CACHE_KEY": "firebase-jwk-cache"
}
```

### Bindings

**KV Namespace** (для кеша Firebase ключей):
```jsonc
"kv_namespaces": [{
  "binding": "PUBLIC_JWK_CACHE_KV",
  "id": "892f29a5888a4e7fb1470dd8ecfff698"
}]
```

**D1 Database**:
```jsonc
"d1_databases": [{
  "binding": "DataBase",
  "database_name": "planer-db",
  "database_id": "95f2e9ac-075e-487b-92ed-623fbc5d8be4"
}]
```

### Capacitor

Настройки мобильного приложения находятся в `capacitor.config.ts`:
- `appId`: `com.planer.app`
- `appName`: `planer`
- `webDir`: `dist/client`

### Cloudflare Workers

Конфигурация Worker находится в `wrangler.jsonc`. Для настройки переменных окружения и bindings см. [документацию Wrangler](https://developers.cloudflare.com/workers/wrangler/configuration/).

## 🗄️ База данных D1

Проект использует Cloudflare D1 для хранения данных о версиях приложения.

### Управление базой данных

```bash
# Применить миграции локально
npm run db:migrate:local

# Применить миграции в production
npm run db:migrate

# Посмотреть список миграций
npm run db:list
```

**База данных:**
- Название: `planer-db`
- ID: `95f2e9ac-075e-487b-92ed-623fbc5d8be4`
- Регион: Production
- Binding: `DataBase`

## 🔐 Аутентификация

Проект использует Firebase Authentication для управления пользователями.

### Функции безопасности

✅ **Email/Password авторизация** - регистрация и вход по email  
✅ **Обязательное подтверждение email** - нельзя войти без подтверждения  
✅ **JWT токены** - автоматическая отправка с каждым запросом  
✅ **Защита API** - все endpoints требуют авторизации  
✅ **Двойная проверка** - frontend + backend validation  

### Процесс авторизации

1. **Регистрация** → Отправка письма подтверждения
2. **Подтверждение email** → Клик по ссылке в письме
3. **Автоматическое перенаправление** → Возврат на сайт
4. **Доступ к приложению** → Email проверен, доступ открыт

### Firebase Configuration

**Project ID:** `planer-3e65e`  
**Auth Domain:** `planer-3e65e.firebaseapp.com`  

Конфигурация находится в `src/firebase/config.ts`

### Authorized Domains

Для работы email verification добавьте в Firebase Console:
- `planer.moldahasank.workers.dev` (production)
- `localhost` (development)

## 🌐 REST API

API для управления версиями приложения. **Все endpoints требуют авторизации.**

### Endpoints

🔒 **Защищенные endpoints:**

- `GET /api/version` - Получить текущую активную версию
- `GET /api/versions` - Получить все версии
- `GET /api/version/:id` - Получить версию по ID
- `POST /api/version` - Создать новую версию
- `PUT /api/version/:id` - Обновить версию
- `DELETE /api/version/:id` - Удалить версию

🌍 **Публичные endpoints:**

- `GET /api/test` - Тест соединения (без авторизации)

### Тестирование API

**С авторизацией:**
```bash
# Получить текущую версию (требуется JWT токен)
curl https://planer.moldahasank.workers.dev/api/version \
  -H "Authorization: Bearer YOUR_JWT_TOKEN"
```

**Без авторизации:**
```bash
# Тест соединения
curl https://planer.moldahasank.workers.dev/api/test
```

### Ошибки авторизации

- **401 Unauthorized** - токен отсутствует или невалиден
- **403 Forbidden** - email не подтвержден

Полная документация API: [API_DOCUMENTATION.md](./API_DOCUMENTATION.md)

## 📝 Скрипты

### Веб-приложение
- `npm run dev` - запуск dev-сервера
- `npm run build` - сборка проекта
- `npm run lint` - проверка кода линтером
- `npm run preview` - предпросмотр production сборки
- `npm run deploy` - развертывание на Cloudflare
- `npm run cf-typegen` - генерация типов для Cloudflare Workers

### База данных
- `npm run db:migrate` - применить миграции в production
- `npm run db:migrate:local` - применить миграции локально
- `npm run db:list` - список миграций

### Мобильное приложение
- `npm run cap:sync` - синхронизация с нативными проектами
- `npm run cap:open:android` - открыть Android Studio
- `npm run cap:android` - сборка + синхронизация + открыть Android Studio

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции (`git checkout -b feature/AmazingFeature`)
3. Закоммитьте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Запушьте в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект находится в разработке.

## 🔗 Ссылки

### Документация

- [React Documentation](https://react.dev)
- [Vite Documentation](https://vite.dev)
- [Cloudflare Workers Documentation](https://developers.cloudflare.com/workers/)
- [Cloudflare D1 Documentation](https://developers.cloudflare.com/d1/)
- [Capacitor Documentation](https://capacitorjs.com/docs)
- [Firebase Auth Documentation](https://firebase.google.com/docs/auth)
- [Hono Documentation](https://hono.dev/)
- [Material-UI Documentation](https://mui.com/)

### Проект

- **Production:** https://planer.moldahasank.workers.dev
- **Firebase Console:** https://console.firebase.google.com/project/planer-3e65e
- **Cloudflare Dashboard:** https://dash.cloudflare.com/

## 🔒 Безопасность

- ✅ Все пароли хешируются Firebase Auth
- ✅ JWT токены проверяются на backend
- ✅ Email verification обязателен
- ✅ CORS настроен правильно
- ✅ Чувствительные данные не логируются

## 🎯 Roadmap

- [x] Базовая авторизация через email/password
- [x] Обязательное подтверждение email
- [x] Защита API endpoints
- [x] JWT токены в localStorage
- [ ] Восстановление пароля
- [ ] OAuth провайдеры (Google, GitHub)
- [ ] Профиль пользователя
- [ ] Роли и права доступа
- [ ] Two-Factor Authentication (2FA)
