import { Box, Typography } from '@mui/material';

export const CalendarPage = () => {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Календарь
      </Typography>
      <Typography>Визуальный календарь (будет реализован позже)</Typography>
    </Box>
  );
};

